version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html

  php-fpm:
    image: php:8.2-fpm
    volumes:
      - .:/var/www/html

  nginx:
    image: nginx:latest
    depends_on:
      - php-fpm

